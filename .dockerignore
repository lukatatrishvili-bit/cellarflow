node_modules
dist
.env
.git
.github
# Runtime database — contains password hashes & OAuth secret. Never bake it into
# the image; it's restored from GCS (or seeded fresh) at runtime.
db.json
data/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.claude
.gemini

# Temporary credential file emitted by google-github-actions/auth. Authentication
# happens before docker build, so this must never enter the image context.
gha-creds-*.json
